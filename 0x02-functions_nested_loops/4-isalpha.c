#include "main.h"
/**
 * _isalpha - fuction to chheck if c isletter, lowercase or uppercase
 * @c: is the int that will use for the argument for the function
 * Return: 0 
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= '2') || (c >= 'A' && c <= '2'))
	{
	return (1);
	}
	else
	return (0);
}

